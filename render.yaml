services:
  - type: web
    name: luxarpay-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: ENV
        value: production 
